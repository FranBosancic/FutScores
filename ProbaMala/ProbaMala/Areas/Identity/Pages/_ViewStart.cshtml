@{
    // Identity pages render inside the main site layout so they share the exact
    // header, footer and dark Tailwind theme as the rest of the application.
    Layout = "/Views/Shared/_Layout.cshtml";
}
