@{
    // All Identity pages (including the default-UI Login/Logout) render inside
    // this self-contained Bootstrap layout, scoping Bootstrap to the auth pages
    // and leaving the main site's Tailwind theme untouched.
    Layout = "/Areas/Identity/Pages/Shared/_Layout.cshtml";
}
