@page
@model LoginModel
@{
    ViewData["Title"] = "Log in";
}

<div class="mx-auto w-full max-w-md px-4 sm:px-6">
    <div class="overflow-hidden rounded-xl border border-slate-800 bg-[#0b0f1a]">

        @* Title bar *@
        <div class="flex items-center gap-2 border-b border-slate-800 bg-slate-800/40 px-4 py-2.5">
            <span class="h-4 w-1 shrink-0 rounded-full bg-lime-500 opacity-70"></span>
            <span class="text-sm font-semibold text-slate-100">Log in</span>
        </div>

        @* Body *@
        <div class="px-4 py-5">
            <p class="mb-5 text-sm text-slate-400">Sign in to your FutScores account.</p>

            <form id="account" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="space-y-5">

                @if (ViewData.ModelState[string.Empty]?.Errors.Count > 0)
                {
                    <div asp-validation-summary="ModelOnly" class="rounded-lg border border-rose-500/30 bg-rose-500/10 px-4 py-3 text-sm text-rose-200" role="alert"></div>
                }

                <div>
                    <label asp-for="Input.Email" class="mb-2 block text-sm font-medium text-slate-200"></label>
                    <input asp-for="Input.Email" autocomplete="username"
                           class="w-full rounded-lg border border-slate-700 bg-slate-900/60 px-3 py-2.5 text-sm text-slate-100 outline-none transition placeholder:text-slate-500 focus:border-lime-500/60 focus:bg-slate-800"
                           placeholder="name@example.com" />
                    <span asp-validation-for="Input.Email" class="mt-2 block text-sm text-rose-300"></span>
                </div>

                <div>
                    <label asp-for="Input.Password" class="mb-2 block text-sm font-medium text-slate-200"></label>
                    <input asp-for="Input.Password" autocomplete="current-password"
                           class="w-full rounded-lg border border-slate-700 bg-slate-900/60 px-3 py-2.5 text-sm text-slate-100 outline-none transition placeholder:text-slate-500 focus:border-lime-500/60 focus:bg-slate-800"
                           placeholder="••••••••" />
                    <span asp-validation-for="Input.Password" class="mt-2 block text-sm text-rose-300"></span>
                </div>

                <label class="flex items-center gap-2 text-sm text-slate-300">
                    <input asp-for="Input.RememberMe"
                           class="h-4 w-4 rounded border-slate-700 bg-slate-900 text-lime-500 focus:ring-1 focus:ring-lime-500/40" />
                    Remember me
                </label>

                <div class="flex flex-wrap items-center gap-3 border-t border-slate-800/60 pt-5">
                    <button id="login-submit" type="submit"
                            class="inline-flex items-center rounded-lg bg-lime-500 px-4 py-2 text-sm font-semibold text-slate-950 transition hover:bg-lime-400">
                        Log in
                    </button>
                    <a asp-area="Identity" asp-page="/Account/Register" asp-route-returnUrl="@Model.ReturnUrl"
                       class="text-sm text-slate-400 transition hover:text-slate-200">
                        Need an account? <span class="font-medium text-lime-400">Register</span>
                    </a>
                </div>
            </form>

            @* External providers (Google, …) — only shown when configured. *@
            @if (Model.ExternalLogins.Count > 0)
            {
                <div class="mt-6">
                    <div class="flex items-center gap-3 text-xs uppercase tracking-wide text-slate-500">
                        <span class="h-px flex-1 bg-slate-800"></span>
                        <span>Or continue with</span>
                        <span class="h-px flex-1 bg-slate-800"></span>
                    </div>

                    <form asp-page="./ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl" method="post" class="mt-4 space-y-3">
                        @foreach (var provider in Model.ExternalLogins)
                        {
                            <button type="submit" name="provider" value="@provider.Name"
                                    title="Log in using your @provider.DisplayName account"
                                    class="flex w-full items-center justify-center gap-3 rounded-lg border border-slate-700 bg-slate-900/60 px-4 py-2.5 text-sm font-medium text-slate-100 transition hover:border-slate-600 hover:bg-slate-800">
                                @if (provider.Name == "Google")
                                {
                                    <svg class="h-5 w-5" viewBox="0 0 24 24" aria-hidden="true">
                                        <path fill="#EA4335" d="M12 10.2v3.9h5.45c-.24 1.4-.97 2.6-2.06 3.4v2.8h3.33C20.66 18.5 21.6 15.6 21.6 12.2c0-.78-.07-1.53-.2-2.25H12z" />
                                        <path fill="#34A853" d="M12 21.6c2.7 0 4.96-.9 6.62-2.43l-3.33-2.58c-.92.62-2.1.98-3.29.98-2.53 0-4.67-1.7-5.44-4h-3.44v2.66C4.4 19.32 7.96 21.6 12 21.6z" />
                                        <path fill="#FBBC05" d="M6.56 13.16c-.2-.6-.31-1.24-.31-1.9 0-.66.11-1.3.31-1.9V6.7H3.12A9.6 9.6 0 0 0 2.4 11.27c0 1.55.37 3.02 1.02 4.32l3.14-2.43z" />
                                        <path fill="#4285F4" d="M12 6.36c1.47 0 2.78.5 3.81 1.49l2.86-2.86C16.96 3.4 14.7 2.4 12 2.4 7.96 2.4 4.4 4.68 2.72 8.04l3.84 2.66c.77-2.3 2.91-4 5.44-4z" />
                                    </svg>
                                }
                                <span>Continue with @provider.DisplayName</span>
                            </button>
                        }
                    </form>
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
