@model DateTimePickerViewModel

<input name="@Model.InputName"
       id="@Model.InputId"
       value="@(Model.Value?.ToString("yyyy-MM-ddTHH:mm:ss") ?? string.Empty)"
       placeholder="@Model.Placeholder"
       class="w-full rounded-lg border border-slate-700 bg-slate-900/60 px-3 py-2.5 text-sm text-slate-100 outline-none transition placeholder:text-slate-500 focus:border-lime-500/60 focus:bg-slate-800"
       data-date-time-picker
       data-enable-time="@Model.EnableTime.ToString().ToLowerInvariant()"
       data-minute-increment="@Model.MinuteIncrement"
       type="text" />