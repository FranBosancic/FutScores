@using ProbaMala.Models.Entities
@model ProbaMala.Models.ViewModels.PlayerDetailsViewModel
@{
    ViewData["Title"] = $"{Model.FirstName} {Model.LastName}";

    @* Age calculation *@
    var age = DateTime.Today.Year - Model.DateOfBirth.Year;
    if (Model.DateOfBirth.Date > DateTime.Today.AddYears(-age)) age--;

    var positionBadgeClass = Model.Position switch
    {
        Position.Goalkeeper => "text-indigo-400 bg-indigo-500/10",
        Position.Defender   => "text-emerald-400 bg-emerald-500/10",
        Position.Midfielder => "text-amber-400 bg-amber-500/10",
        Position.Forward    => "text-rose-400 bg-rose-500/10",
        _                   => "text-slate-400 bg-slate-500/10"
    };
}

<div class="mx-auto w-full max-w-3xl space-y-4 px-4 sm:px-6">

    @* Header panel *@
    <div class="overflow-hidden rounded-xl border border-slate-800 bg-[#0b0f1a]">

        @* Title bar *@
        <div class="flex items-center justify-between border-b border-slate-800 bg-slate-800/40 px-4 py-2.5">
            <div class="flex items-center gap-2">
                <span class="h-4 w-1 shrink-0 rounded-full bg-lime-500 opacity-70"></span>
                <span class="text-sm font-semibold text-slate-100">@($"{Model.FirstName} {Model.LastName}")</span>
                <span class="mx-1 text-slate-600">/</span>
                <a asp-route="players-index" class="text-xs text-slate-400 transition hover:text-slate-200">All players</a>
            </div>
            <div class="flex items-center gap-2">
                <a asp-route="player-edit" asp-route-id="@Model.Id"
                   class="flex items-center gap-1 rounded px-2 py-1 text-xs text-slate-400 transition hover:bg-slate-700 hover:text-slate-100">
                    <svg class="h-3.5 w-3.5" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
                        <path d="M2.695 14.763l-1.262 3.154a.5.5 0 00.65.65l3.155-1.262a4 4 0 001.343-.885L17.5 5.5a2.121 2.121 0 00-3-3L3.58 13.42a4 4 0 00-.885 1.343z" />
                    </svg>
                    Edit
                </a>
                <a asp-route="player-delete" asp-route-id="@Model.Id"
                   class="flex items-center gap-1 rounded px-2 py-1 text-xs text-slate-400 transition hover:bg-rose-500/10 hover:text-rose-400">
                    <svg class="h-3.5 w-3.5" fill="currentColor" viewBox="0 0 20 20">
                        <path fill-rule="evenodd" d="M8.75 1A2.75 2.75 0 006 3.75v.443c-.795.077-1.584.176-2.365.298a.75.75 0 10.23 1.482l.149-.022.841 10.518A2.75 2.75 0 007.596 19h4.807a2.75 2.75 0 002.742-2.53l.841-10.52.149.023a.75.75 0 00.23-1.482A41.03 41.03 0 0014 4.193V3.75A2.75 2.75 0 0011.25 1h-2.5zM10 4c.84 0 1.673.025 2.5.075V3.75c0-.69-.56-1.25-1.25-1.25h-2.5c-.69 0-1.25.56-1.25 1.25v.325C8.327 4.025 9.16 4 10 4zM8.58 7.72a.75.75 0 00-1.5.06l.3 7.5a.75.75 0 101.5-.06l-.3-7.5zm4.34.06a.75.75 0 10-1.5-.06l-.3 7.5a.75.75 0 101.5.06l.3-7.5z" clip-rule="evenodd" />
                    </svg>
                    Delete
                </a>
            </div>
        </div>

        @* Stat chips row *@
        <div class="flex flex-wrap items-center gap-3 px-4 py-4">

            @* Position badge *@
            <div class="flex items-center gap-1.5 rounded-lg bg-slate-800/60 px-3 py-1.5">
                <span class="text-xs text-slate-500">Position</span>
                <span class="rounded px-2 py-0.5 text-xs font-medium @positionBadgeClass">
                    @Model.Position.ToString()
                </span>
            </div>

            @* Club *@
            <div class="flex items-center gap-1.5 rounded-lg bg-slate-800/60 px-3 py-1.5">
                <span class="text-xs text-slate-500">Club</span>
                @if (Model.ClubId != 0)
                {
                    <a asp-route="club-details" asp-route-id="@Model.ClubId"
                       class="text-sm font-semibold text-lime-400 transition hover:text-lime-300">
                        @(Model.ClubName ?? "—")
                    </a>
                }
                else
                {
                    <span class="text-sm font-semibold text-slate-100">—</span>
                }
            </div>

            @* Nationality *@
            <div class="flex items-center gap-1.5 rounded-lg bg-slate-800/60 px-3 py-1.5">
                <span class="text-xs text-slate-500">Nationality</span>
                <span class="text-sm font-semibold text-slate-100">@(Model.Nationality ?? "—")</span>
            </div>

            @* Age *@
            <div class="flex items-center gap-1.5 rounded-lg bg-slate-800/60 px-3 py-1.5">
                <span class="text-xs text-slate-500">Age</span>
                <span class="text-sm font-semibold text-slate-100">@age</span>
            </div>

            @* Ratings *@
            <div class="flex items-center gap-1.5 rounded-lg bg-slate-800/60 px-3 py-1.5">
                <span class="text-xs text-slate-500">Ratings</span>
                <span class="text-sm font-semibold text-slate-100">@Model.RatingCount</span>
            </div>
        </div>
    </div>

    @* Recent Matches panel — derived from ratings data *@
    @{
        var recentMatches = Model.Ratings?
            .GroupBy(r => r.MatchId)
            .Select(g => new {
                MatchId      = g.Key,
                HomeTeamName = g.First().HomeTeamName,
                AwayTeamName = g.First().AwayTeamName,
                MatchScore   = g.First().MatchScore,
                MatchDate    = g.First().MatchDate,
                AvgScore     = g.Average(r => (double)r.Score),
                RatingCount  = g.Count()
            })
            .OrderByDescending(m => m.MatchDate)
            .ToList();
    }

    <div class="overflow-hidden rounded-xl border border-slate-800 bg-[#0b0f1a]">
        <div class="border-b border-slate-800 bg-slate-800/40 px-4 py-2.5">
            <span class="text-xs font-semibold uppercase tracking-widest text-slate-400">Recent Matches</span>
        </div>

        @if (recentMatches == null || !recentMatches.Any())
        {
            <div class="px-4 py-8 text-center text-sm text-slate-500">No rated matches yet.</div>
        }
        else
        {
            <div class="grid grid-cols-[1fr_4.5rem_3rem] items-center gap-x-3 border-b border-slate-800/60 px-4 py-2 text-xs font-semibold uppercase tracking-widest text-slate-600">
                <span>Match</span>
                <span class="text-right">Date</span>
                <span class="text-right">Rtg</span>
            </div>

            @foreach (var matchRow in recentMatches)
            {
                var avgBadgeClass = matchRow.AvgScore switch
                {
                    >= 9 => "bg-emerald-500/15 text-emerald-300",
                    >= 7 => "bg-lime-500/15 text-lime-300",
                    >= 5 => "bg-amber-500/15 text-amber-300",
                    _    => "bg-rose-500/15 text-rose-300"
                };

                <a asp-route="match-details" asp-route-id="@matchRow.MatchId"
                   class="grid grid-cols-[1fr_4.5rem_3rem] items-center gap-x-3 border-b border-slate-800/40 px-4 py-3 last:border-b-0 transition hover:bg-white/[0.03]">
                    <div class="flex min-w-0 items-center gap-2">
                        <span class="h-4 w-1 shrink-0 rounded-full bg-lime-500 opacity-70"></span>
                        <span class="truncate text-sm font-medium text-slate-100">@matchRow.HomeTeamName</span>
                        <span class="shrink-0 rounded bg-slate-800 px-1.5 py-0.5 text-xs font-bold tabular-nums text-slate-200">@matchRow.MatchScore</span>
                        <span class="truncate text-sm font-medium text-slate-100">@matchRow.AwayTeamName</span>
                    </div>
                    <span class="text-right text-xs text-slate-500">@matchRow.MatchDate.ToString("MMM d")</span>
                    <div class="flex justify-end">
                        <span class="inline-flex h-7 w-7 items-center justify-center rounded-full text-xs font-bold @avgBadgeClass"
                              title="@matchRow.RatingCount rating(s)">
                            @matchRow.AvgScore.ToString("0.0")
                        </span>
                    </div>
                </a>
            }
        }
    </div>

    @* Ratings panel *@
    <div class="overflow-hidden rounded-xl border border-slate-800 bg-[#0b0f1a]">
        <div class="flex items-center justify-between border-b border-slate-800 bg-slate-800/40 px-4 py-2.5">
            <span class="text-xs font-semibold uppercase tracking-widest text-slate-400">Ratings</span>
            <a asp-route="rating-create" class="flex items-center gap-1 text-xs font-semibold text-lime-400 transition hover:text-lime-300">
                <svg class="h-3.5 w-3.5" fill="currentColor" viewBox="0 0 20 20">
                    <path d="M10.75 4.75a.75.75 0 00-1.5 0v4.5h-4.5a.75.75 0 000 1.5h4.5v4.5a.75.75 0 001.5 0v-4.5h4.5a.75.75 0 000-1.5h-4.5v-4.5z" />
                </svg>
                Add rating
            </a>
        </div>
        @{ ViewData["RatingHide"] = "player"; }
        <partial name="_RatingMiniList" model="Model.Ratings" />
    </div>

</div>
